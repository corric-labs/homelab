# Copy this file to .env for local testing only.
# Do not commit real values.

GITHUB_OWNER=corric-labs
GITHUB_REPO=homelab

# MinIO backend (example placeholders)
TF_STATE_ENDPOINT=https://minio.example.internal
TF_STATE_BUCKET=homelab-tofu-state
TF_STATE_REGION=us-east-1

# API endpoints (example placeholders)
PROXMOX_API_URL=https://proxmox.example.internal:8006/api2/json
UNIFI_API_URL=https://udm-pro.example.internal
